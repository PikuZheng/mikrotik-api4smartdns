name: build
on:
  workflow_dispatch:
      
jobs:
  build: 
    strategy:
      matrix:
        goarch: [x86_64, aarch64, armv7]
      fail-fast: false
    runs-on: ubuntu-latest
    env:
      GOARCH: ${{ matrix.goarch }}
    steps:
      - uses: actions/checkout@v3
        with:
          repository: PikuZheng/smartdns
          path: smartdns
      - uses: actions/checkout@v3
        with:
          path: smartdns/plugin
      - uses: docker/setup-qemu-action@v2
      - uses: docker/setup-buildx-action@v2
        with:
          install: true
      - name: Extract version from Cargo.toml
        id: extract_version
        run: |
          cd ${{ github.workspace }}/smartdns/plugin/mikrotik-api
          VERSION=$(grep '^version =' Cargo.toml | sed 's/version = "\(.*\)"/\1/')
          echo "CUR_VERSION=$VERSION" >> $GITHUB_ENV
      - name: build
        run: |
          cd ${{ github.workspace }}/smartdns/plugin/mikrotik-api
          rm docker-build.sh || true
          cat >> docker-build.sh << EOF
          cd /workdir/smartdns/plugin/mikrotik-api
          export RUSTFLAGS='-C link-arg=-Wl,-rpath,./ -C target-feature=-crt-static'
          mkdir -p build
          if  test "$GOARCH" = "armv7" ;then
            cargo build --release --target=armv7-unknown-linux-musleabihf
            cp ./target/armv7-unknown-linux-musleabihf/release/libmikrotik_api.so ./build
          else
            cargo build --release --target=$GOARCH-unknown-linux-musl
            cp ./target/$GOARCH-unknown-linux-musl/release/libmikrotik_api.so ./build
          fi
          EOF
          if  test "$GOARCH" = "armv7" ;then
            docker run -i --rm -v ${{ github.workspace }}:/workdir messense/rust-musl-cross:armv7-musleabihf sh /workdir/smartdns/plugin/mikrotik-api/docker-build.sh
          else
            docker run -i --rm -v ${{ github.workspace }}:/workdir messense/rust-musl-cross:$GOARCH-musl sh /workdir/smartdns/plugin/mikrotik-api/docker-build.sh
          fi
      - name: general tag and release file
        id: tag
        run: |
          cd ${{ github.workspace }}/smartdns/plugin/mikrotik-api
          echo "release_tag=$GOARCH-${{ env.CUR_VERSION }}" >> $GITHUB_OUTPUT
          touch release.txt
          echo ${{ github.sha }} >> release.txt
          echo "status=success" >> $GITHUB_OUTPUT
      - name: Upload firmware to release
        uses: softprops/action-gh-release@v1
        if: steps.tag.outputs.status == 'success'
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN }}
        with:
          tag_name: ${{ steps.tag.outputs.release_tag }}
          body_path: ${{ github.workspace }}/smartdns/plugin/mikrotik-api/release.txt
          files: |
            ${{ github.workspace }}/smartdns/plugin/mikrotik-api/build/*
